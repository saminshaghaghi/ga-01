name: 1-hello-world

on: push

jobs:
    first-job:
        runs-on: ubuntu-latest
        steps:
            - name: print hello
              run: echo "Hello World"

            - name: print goodbye
              run: echo "GoodBye"  
